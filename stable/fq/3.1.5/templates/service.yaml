{{- if (.Values.config.api_server.enabled) }}
apiVersion: v1
kind: Service
metadata:
  name: freqtrade
  labels:
    app: freqtrade
spec:
  type: NodePort
  ports:
  - protocol: TCP
    port: 8080
    nodePort: 30080
    targetPort: api
  selector:
    app: freqtrade
{{- end }}
